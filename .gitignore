# Xcode / macOS build artifacts
build/
DerivedData/
*.xcodeproj/          # generated by `xcodegen generate` on the Mac / in CI
*.xcworkspace/
*.xcuserstate
xcuserdata/
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (not used, but ignore just in case)
Pods/
Carthage/Build/

# Secrets — never commit signing material or API keys
*.p12
*.mobileprovision
*.cer
AuthKey_*.p8
